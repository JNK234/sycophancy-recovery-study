# API Keys for sycophantic data generation
# Copy this file to .env and fill in your keys

# OpenAI API key (for gpt-4o-mini responses)
OPENAI_API_KEY=sk-...

# Anthropic API key (for claude-3-haiku responses)
ANTHROPIC_API_KEY=sk-ant-...

# Google API key (for gemini responses)
GOOGLE_API_KEY=...

# HuggingFace token (for dataset upload and model downloads)
HF_TOKEN=hf_...

# =============================================================================
# CLUSTER/HPC SETTINGS (Important for avoiding quota issues)
# =============================================================================

# HuggingFace cache directory - SET THIS ON CLUSTERS!
# Models download here. Default ~/.cache/huggingface uses home directory quota.
# Point to scratch/work directory with sufficient space (50GB+ recommended).
HF_HOME=/scratch/your_username/.cache/huggingface

# Disable vLLM usage statistics (prevents disk writes that can hit quota)
VLLM_NO_USAGE_STATS=1

# =============================================================================
# vLLM settings (optional environment variable overrides)
# =============================================================================
# These can also be set in configs/generation.py VLLMInferenceConfig
# VLLM_TENSOR_PARALLEL_SIZE=2
# VLLM_GPU_MEMORY_UTILIZATION=0.9
